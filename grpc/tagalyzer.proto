syntax = "proto2";
option java_package = "io.tagalyzer.posts";

package debate;

service Tagalyzer {
  rpc Post (PostRequest) returns () {}
}

message PostRequest {
  optional string client_id = 1;
  optional string text = 2;
  optional uint64 created_at = 3;
}
